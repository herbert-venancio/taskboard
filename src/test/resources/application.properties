spring.profiles.active=test

#SECURITY
security.ignored=/static/**
#STATIC
spring.mvc.static-path-pattern=/static/**

#EMBEDDED SERVER
server.session.timeout=-1
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain

#DATASOURCE
# Create DDL
spring.jpa.hibernate.ddl-auto=create

# H2 in local file system allowing other simultaneous connections
spring.datasource.url=jdbc:h2:file:./db2;AUTO_SERVER=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.platform=h2

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
flyway.enabled=false

#LOGGING
logging.level.org.springframework.security=INFO
logging.level.org.springframework.boot.actuate.audit.listener.AuditListener=DEBUG
logging.level.org.apache.http=WARN

#flyway
# add the following line with the appropriate database
#flyway.locations=db/migration/oracle

# add the following lines to projects that already exists before flyway was added
#flyway.baseline-on-migrate=true
#flyway.baselineVersionAsString=1

spring.http.multipart.max-file-size=100Mb
spring.http.multipart.max-request-size=100Mb