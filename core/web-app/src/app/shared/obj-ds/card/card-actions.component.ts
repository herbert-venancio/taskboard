import {Component} from '@angular/core';

/**
 * Component intended to be used within the `<obj-card>` component.
 */
@Component({
    selector: 'obj-card-actions',
    template: '<ng-content></ng-content>'
})
export class CardActionsComponent {
}
